import React from 'react';

/**
 * Fixed grain texture overlay — gives the cream paper its subtle, filmic
 * feel. SVG-based so it scales infinitely without a raster asset.
 */
export default function GrainOverlay() {
  return <div className="grain" aria-hidden="true" />;
}
